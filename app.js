const menu=document.getElementById('menu');const nav=document.getElementById('nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));const q=document.getElementById('q'),loc=document.getElementById('loc'),grid=document.getElementById('jobGrid'),none=document.getElementById('none');function filterJobs(){const a=(q.value||'').toLowerCase().trim(),b=(loc.value||'').toLowerCase().trim();let shown=0;grid.querySelectorAll('.job').forEach(card=>{const ok=(!a||card.dataset.title.includes(a))&&(!b||card.dataset.location.includes(b));card.style.display=ok?'block':'none';if(ok)shown++});none.classList.toggle('hidden',shown!==0)}document.getElementById('searchBtn')?.addEventListener('click',filterJobs);[q,loc].forEach(x=>x?.addEventListener('keydown',e=>{if(e.key==='Enter')filterJobs()}));document.querySelectorAll('.outline').forEach(btn=>btn.addEventListener('click',()=>alert('This opportunity detail page will be connected to the live job database in the next phase.')));